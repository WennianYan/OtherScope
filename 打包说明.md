# OtherScope 一键打包说明（build 文件夹）

源码根目录下的 **`build/`** 文件夹包含 **三个平台的打包脚本**。
**每次源码更新后，用户只需双击对应脚本，即自动完成打包，生成最终的单个运行文件**，
全程无需手动安装依赖或执行其他命令。

---

## 一、三个打包脚本（双击即打包）

| 平台 | 脚本（位于 build/） | 双击方式 | 生成产物 |
|---|---|---|---|
| **Windows** | `build_windows.bat` | 双击 | `dist\OtherScope.exe`（单文件，双击即运行） |
| **Linux** | `build_linux.sh` | 双击（已授权可执行）；或终端 `./build/build_linux.sh` | `dist/OtherScope`（单文件） |
| **macOS** | `build_macos.command` | 双击（自动在终端运行） | `dist/OtherScope.app`（双击即运行） |

> Windows / macOS 的 exe / app 需要在对应系统上运行脚本生成（打包工具 PyInstaller
> 不支持跨平台交叉编译）。Linux 脚本在本仓库已实测一键打包通过。
> 另附 `.github/workflows/build.yml`：推送到 GitHub 打 tag（v/V 开头均可）即可云端
> 同时构建三个平台的单文件，并**自动创建 GitHub Release**，把 Windows / macOS / Linux
> 三个产物作为附件发布到 Releases 页面（仓库首页右侧即可看到三平台下载入口）。

## 二、脚本自动处理的内容（用户无需任何操作）

每个脚本全自动完成以下步骤，并处理所有能想到的异常/依赖：

1. **环境检测**
   - Windows：自动检测 `python` / `py`；缺失时用 `winget` 自动安装 Python 3.12
   - Linux：自动检测 `python3`；缺失时用 `apt`/`dnf` 自动安装
   - macOS：自动检测 Xcode 命令行工具与 Python；缺失时自动安装（Homebrew）
2. **依赖自动安装**
   - 升级 pip → 安装 requirements.txt 全部运行依赖 + PyInstaller
   - 默认源失败自动切换清华镜像重试
   - 优先创建隔离虚拟环境（`.build_venv`），避免污染系统 Python
3. **系统级依赖自动安装（Linux）**
   - 检测 Qt 运行库（libGL / libEGL / libxkbcommon / libxcb-cursor），缺失自动 `apt`/`dnf` 安装
4. **环境检查**
   - 磁盘空间检查（不足 2.5GB 时明确提示）
   - 构建目录 / 旧产物自动清理（不残留过期文件）
5. **一键打包**
   - PyInstaller `--onefile --windowed` 生成单个可执行文件（依赖全部内嵌）
6. **结果提示**
   - 成功：显示产物路径并自动打开所在文件夹
   - 失败：明确列出原因（网络 / 杀软拦截 / 内存 / 磁盘）与修复指引，绝不静默

## 三、运行产物自带自举环境修复

生成的单文件程序（三个平台一致）双击运行时，自动执行 `bootstrap.py` 自举逻辑：
检测目标系统缺失的 Qt 运行库 → 自动下载安装 → 自动重启程序 → 异常图形弹窗提醒，
绝不静默失败。

## 四、目录结构（纯净源码）

```
源码根目录/
├── build/                    一键打包脚本
│   ├── build_windows.bat     Windows -> dist\OtherScope.exe
│   ├── build_linux.sh        Linux   -> dist/OtherScope
│   └── build_macos.command    macOS   -> dist/OtherScope.app
├── .github/workflows/build.yml  GitHub Actions 三平台云端自动构建
├── main.py                   程序入口（含自举调用）
├── launcher.py               源码运行引导（自动装依赖）
├── bootstrap.py              自举启动器（运行环境自动修复）
├── requirements.txt          依赖清单
├── run_linux.sh / run_macos.command / run_windows.bat   源码一键运行
├── 打包说明.md               本文档
└── OtherScope/               全部源码模块
```

## 五、版本信息

- 版本：V1.0（初始发布）
- 构建：PyInstaller `--onefile --windowed`，依赖与 Qt 运行库全部内嵌单文件
- 依赖：Python 3.9+ · PySide6>=6.8 · pyqtgraph>=0.14.0 · pyserial · numpy
